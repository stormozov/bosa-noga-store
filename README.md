# Интернет-магазин обуви

[![Deploy frontend to GitHub Pages](https://github.com/stormozov/bosa-noga-store/actions/workflows/deploy.yml/badge.svg)](https://github.com/stormozov/bosa-noga-store/actions/workflows/deploy.yml)

---

## 📌 Описание проекта

Одностраничное приложение (SPA) интернет-магазина обуви с полным циклом пользовательского взаимодействия: каталог товаров, детальные карточки, корзина с персистентным хранением, поиск, оформление заказов. Приложение реализовано в соответствии с предоставленной версткой с небольшими доработками и спецификацией API. Все сетевые операции сопровождаются обработкой состояний загрузки и ошибок. Корзина сохраняется в `localStorage` с фиксацией цен на момент добавления.

---

## ✨ Ключевые функции

- **Роутинг**: Реализован через React Router
- **Каталог товаров**:
  - Фильтрация по категориям (включая динамически добавленную опцию «Все»)
  - Пагинация (загрузка по 6 элементов, кнопка «Загрузить ещё»)
  - Поиск по названию (частичное совпадение) и цвету (точное, без учёта регистра)
- **Страница товара** (`/catalog/:id`):
  - Выбор доступного размера (только `available: true`)
  - Валидация: кнопка «В корзину» активна только при выборе размера
  - Фиксация цены в `localStorage` на момент добавления
- **Корзина**:
  - Учёт позиций как «товар + размер»
  - Удаление позиций с синхронной очисткой `localStorage`
  - Автоматический расчёт итоговой суммы
  - Оформление заказа через `POST /api/order` с последующей очисткой корзины
- **Глобальный поиск**: Иконка (виджет) в шапке → раскрытие поля → переход на `/catalog` с применённым запросом
- **Обработка состояний**:
  - Лоадеры
  - Сообщения об ошибках (сетевые сбои, пустые результаты)
  - Скрытие блоков при отсутствии данных

---

## 🛠 Технологический стек фонтенда

| Категория       | Технологии                                               |
| --------------- | -------------------------------------------------------- |
| **Основа**      | React 19, React Router 7                                 |
| **Состояние**   | Redux Toolkit                                            |
| **Хранение**    | `localStorage` (корзина с сериализацией/десериализацией) |
| **Стили**       | SCSS (адаптация предоставленной верстки из `html`)       |
| **Инструменты** | ESLint, Prettier, Biome                                  |
| **Сборка**      | Vite                                                     |

> _Выбор Redux Toolkit обусловлен необходимостью централизованного управления состоянием корзины._

---

## 🚀 Установка и запуск

### Локальный запуск

```bash
# Клонирование репозитория

git clone https://github.com/stormozov/bosa-noga-store.git
cd bosa-noga-store
```

```bash
# Бэкенд: Установка зависимостей и запуск

cd backend
npm install
npm run start
```

```bash
# Фронтенд: Установка зависимостей и запуск в режиме разработки (порт 5173)

cd frontend
yarn install
yarn start
```

### Настройка API

1. Настройка переменных окружения:

- Необходимо создать два `.env` файла: `.env.development` и `.env.production`.
- Базовый URL бэкенда задан через переменную окружения:

  ```env
  VITE_API_BASE_URL=https://ra16-diploma-1.onrender.com
  ```

- Базовое имя (используется для настройки роутинга в продакшене для развертывания на Github Pages):

  ```env
  VITE_BASENAME=/bosa-noga-store/
  ```

2. Для локального тестирования обработки ошибок/задержек:

Запустите бэкенд с флагами:

```bash
APP_DELAY=true APP_ERROR=true node src/backend/index.mjs
```

или

```bash
yarn run flaky
```

> ⚠️ Публичный бэкенд `https://bosa-noga-store.onrender.com` работает на бесплатном тарифе Render — возможны задержки инициализации после простоя.

---

## 📂 Структура проекта

Проект разделен на две части:
- **Бэкенд**: Node.js
- **Фронтенд**: React + Vite

Дополнительно:
- **html**: статичные файлы с исходной версткой
- **task**: файлы задач проекта

---

## 🔐 Особенности реализации

### Корзина

- Хранение структуры:
  ```json
  {
    "cartItems": [
      {
        "id": 42,
        "size": 38,
        "title": "Кроссовки Urban",
        "price": 5990,
        "count": 2,
        "image": "/img/42-1.jpg"
      }
    ]
  }
  ```
- Цена фиксируется при добавлении — не зависит от последующих изменений на сервере.
- При оформлении заказа: валидация данных → отправка на `/api/order` → очистка состояния и `localStorage`.

### Обработка ошибок

- Для каждого критичного запроса (каталог, товар, заказ) реализованы:
  - Состояние `isLoading` → отображение локального лоадера
  - Состояние `error` → показ сообщения с кнопкой повтора запроса
  - Обработка сетевых ошибок

### Поиск

- Глобальный поиск: переход на `/catalog?q=текст` с сохранением значения в поле
- На странице каталога: комбинированный запрос `?q=...&categoryId=...&offset=...`
- При пустом результате — отображение информативного сообщения «Товары не найдены»
